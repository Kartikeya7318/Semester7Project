<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@android:color/transparent" />
    <!-- Change the color above to the background color you want inside the rectangle -->

    <stroke
        android:width="2dp"
    android:color="?attr/colorPrimary" />
    <corners android:radius="8dp" /> <!-- Adjust the radius to round corners, if needed -->
</shape>